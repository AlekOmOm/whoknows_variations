global:
  resolve_timeout: 5m

route:
  group_by: ['cluster','alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h
  receiver: oncall

receivers:
- name: oncall
  pagerduty_configs:
  - service_key: ${PAGERDUTY_SERVICE_KEY}
    url: ${PAGERDUTY_URL}
    send_resolved: true 